<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.login_page">

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_shoppingcart"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".softdrinks"></activity>
        <activity
            android:name=".ScrollingActivity"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".mango" />
        <activity android:name=".imageupload" />
        <activity android:name=".admin_products" />
        <activity android:name=".admin" />
        <activity android:name=".hygiene" />
        <activity android:name=".fruit" />
        <activity android:name=".Home" />
        <activity android:name=".sign_in" />
        <activity android:name=".forgetpassword" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>